import { SectionHeader } from '../../components/ui';

export function CrossSellSection() {
  return (
    <div>
      <SectionHeader
        eyebrow="Cross-Sell"
        title="Cross-Sell"
        sub="AR cross-sell plays — content coming soon."
      />
    </div>
  );
}
